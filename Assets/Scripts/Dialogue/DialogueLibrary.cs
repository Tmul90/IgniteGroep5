using System.Collections.Generic;

public static class DialogueLibrary
{
    public static Dictionary<string, DialogueData> _dialogueDict = new(); // Dictionary with the first value the Dialogue ID and the second the DialogueData
}
